config {
    testsDir "tests"
    workDir ".nf-test"
    configFile "nextflow.config"
    profile "test_stub,docker"

    // Global settings
    stage {
        copy "assets/**"
        copy "conf/**"
        copy "modules/**"
        copy "subworkflows/**"
        copy "workflows/**"
    }

}
