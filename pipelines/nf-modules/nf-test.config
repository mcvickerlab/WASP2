config {
    // Location of nf-modules tests
    testsDir "."

    // Location of test data
    configFile "nextflow.config"

    // Profile for running tests
    profile "docker"

    // Plugins
    plugins {
        load "nft-utils@0.0.3"
    }
}
