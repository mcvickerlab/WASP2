# Test Data Bundle

`wasp2_test_bundle.tar.gz` packages the NA12878 chr10 regression dataset (BAM/BED/VCF/as_counts).

Store this tarball in releases or keep it locally; the repo should not expand its contents.
